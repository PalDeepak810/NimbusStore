server:
  port: 8082

spring:
  application:
    name: demo

  datasource:
    url: jdbc:postgresql://localhost:5432/nimbus_metadata
    username: postgres
    password: Dp810@310

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true